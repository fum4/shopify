query CartInput {
  cart {
    lines {
      id
      cost {
        subtotalAmount {
          amount
        }
      }
    }
    buyerIdentity {
      customer {
        hasAnyTag(tags: ["VIP"])
      }
    }
  }
  discount {
    discountClasses
  }
}
